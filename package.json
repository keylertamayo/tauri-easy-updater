{
  "name": "tauri-easy-updater-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo for tauri-easy-updater core library, CLI, and Tauri plugin.",
  "repository": "https://github.com/keylertamayo/tauri-easy-updater",
  "license": "MIT",
  "scripts": {
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-react-hooks": "^5.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.6.0"
  },
  "workspaces": [
    "packages/*",
    "crates/*"
  ]
}

